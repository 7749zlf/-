# -
## Short Video Backend

Spring Boot backend for the short drama project.

### Run

```bash
./mvnw spring-boot:run
```

### Notes

- MySQL is configured in `src/main/resources/application.properties`.
- Build output, logs, and local database backups are ignored by git.
